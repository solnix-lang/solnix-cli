[package]
name = "solnix-cli"
version = "0.1.0"
edition = "2021"
description = "Solnix CLI: frontend for Solnix compiler"
license = "Apache-2.0"

[dependencies]
clap = { version = "4.4", features = ["derive"] }
colored = "2.0"   # optional for colored welcome UI
